#include <stdio.h>
#include <math.h>
/**
 * main - prints the main function
 *
 * Return: 0 (Always success)
 */
int main(void)
{
	double x, y;

	x = 2, y = 7;

	printf("pow(2, 7) returns: %7.0f\n", pow(x,y));
	
	return (0);
}
