#include "main.h"
/**
 * mul - multiplies two integer
 * @a: first integer
 * @b: second integer
 *
 * Return: 0 (Always success)
 */
int mul(int a, int b)
{
	int result;

	result = (a * b);

	return (result);
}
